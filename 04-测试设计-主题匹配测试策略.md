# 测试设计：MQTT 主题匹配测试策略

## 测试方法论

采用 **Harness Engineering** + **分层测试** 设计思想，按照 MQTT 规范拆解测试维度，确保每个规则点都被充分验证。

---

## 测试维度拆解

### 维度 1：等价类划分

| 等价类 | 说明 | 测试用例 |
|--------|------|---------|
| 精确匹配类 | 无通配符，完全相等 | `sensor/temp` == `sensor/temp` |
| + 单层匹配 | + 恰好匹配一个层级 | `sensor/+` → `sensor/temp` |
| # 多层匹配 | # 匹配多个后续层级 | `sensor/#` → `sensor/a/b/c` |
| # 零层匹配 | # 匹配零个后续层级 | `sensor/#` → `sensor` |
| 组合匹配 | + 与 # 同时使用 | `sensor/+/#` → `sensor/room/temp` |
| 不匹配类 | 各种不匹配场景 | 层级不匹配、前缀不匹配等 |

### 维度 2：边界值分析

| 边界场景 | 风险点 | 测试用例 |
|---------|--------|---------|
| 空主题 | 空字符串的层级定义 | `#` → `""` ✓, `+` → `""` ✗ |
| 空前导层级 | `/sensor` 的第一个层级为空 | `/sensor` vs `sensor` |
| 空末尾层级 | `sensor/` 的最后一个层级为空 | `sensor/` vs `sensor` |
| 单层边界 | `+` 恰好一层，不多不少 | `a/+` → `a/b` ✓, `a/+` → `a` ✗ |
| 大小写敏感 | MQTT 规范区分大小写 | `Sensor` vs `sensor` |

### 维度 3：错误推测法

| 易出错场景 | 错误假设 | 验证点 |
|-----------|---------|--------|
| fnmatch 陷阱 | `*` 跨层级匹配 | `a/+` 不能匹配 `a/b/c` |
| split 空字符串 | `"".split("/")` → `[""]` | 空主题是 0 层还是 1 层？ |
| # 在非末尾 | MQTT 规范不允许但代码如何处理 | `a/#/b` 作为字面量匹配 |

---

## 测试用例设计矩阵

### A. 基础匹配测试 (3 类)

| ID | 模式 | 主题 | 预期 | 验证点 |
|----|------|------|------|--------|
| A1 | `sensor/temp` | `sensor/temp` | ✓ | 精确匹配 |
| A2 | `sensor/temp` | `sensor/hum` | ✗ | 精确不匹配 |
| A3 | `sensor/temp` | `sensor/temp/val` | ✗ | 层级不匹配 |

### B. `#` 多层通配符测试 (4 类)

| ID | 模式 | 主题 | 预期 | 验证点 |
|----|------|------|------|--------|
| B1 | `#` | `any/topic/here` | ✓ | # 单独匹配所有 |
| B2 | `sensor/#` | `sensor` | ✓ | # 匹配零层级 |
| B3 | `sensor/#` | `sensor/temp` | ✓ | # 匹配一层 |
| B4 | `sensor/#` | `sensor/temp/room1/val` | ✓ | # 匹配多层 |

### C. `+` 单层通配符测试 (5 类)

| ID | 模式 | 主题 | 预期 | 验证点 |
|----|------|------|------|--------|
| C1 | `sensor/+` | `sensor/temp` | ✓ | + 匹配单层 |
| C2 | `sensor/+` | `sensor` | ✗ | + 不能匹配零层 |
| C3 | `sensor/+` | `sensor/temp/val` | ✗ | + 不能跨层 |
| C4 | `+/+` | `a/b` | ✓ | 多个 + 匹配 |
| C5 | `+/+` | `a/b/c` | ✗ | 层级数量校验 |

### D. 组合通配符测试 (3 类)

| ID | 模式 | 主题 | 预期 | 验证点 |
|----|------|------|------|--------|
| D1 | `sensor/+/#` | `sensor/room1` | ✓ | + 后接 # 零层 |
| D2 | `sensor/+/#` | `sensor/room1/temp` | ✓ | + 后接 # 一层 |
| D3 | `sensor/+/#` | `sensor/room1/temp/val` | ✓ | + 后接 # 多层 |

### E. 边界与特殊情况 (6 类)

| ID | 模式 | 主题 | 预期 | 验证点 |
|----|------|------|------|--------|
| E1 | `/sensor` | `/sensor` | ✓ | 前导 / 空层级 |
| E2 | `/sensor` | `sensor` | ✗ | 前导 / 与无前导 |
| E3 | `topic` | `topic/` | ✗ | 末尾 / 空层级 |
| E4 | `Sensor/Temp` | `sensor/temp` | ✗ | 大小写敏感 |
| E5 | `#` | `""` | ✓ | # 匹配空主题 |
| E6 | `+` | `""` | ✗ | + 不匹配空主题 |

### F. 真实场景验证 (2 类)

| ID | 模式 | 主题 | 预期 |
|----|------|------|------|
| F1 | `devices/+/telemetry/+` | `devices/sensor001/telemetry/temp` | ✓ |
| F2 | `building/+/floor/+/sensor/#` | `building/a/floor/3/sensor/temp/val` | ✓ |

---

## 测试覆盖率目标

| 覆盖类型 | 目标 | 实际 |
|---------|------|------|
| 语句覆盖 | 100% | ✅ |
| 分支覆盖 | 100% | ✅ |
| 路径覆盖 | 关键路径全覆盖 | ✅ |
| MQTT 规范点 | 所有规则点 | ✅ |

---

## 测试驱动开发流程

```
1. 先写测试用例（15 个）
   ↓
2. 运行测试 → 8 个失败
   ↓
3. 实现算法（重写 _match_topic）
   ↓
4. 运行测试 → 1 个失败（空主题边界）
   ↓
5. 修正边界处理
   ↓
6. 运行测试 → 全部通过 ✓
```

---

## 防回归设计

原有 `test_message_bus.py` 的 11 个测试作为**回归测试套件**，确保重构不破坏现有功能：
- 发布订阅基础流程
- 服务请求响应
- 取消订阅
- 并发消息投递
