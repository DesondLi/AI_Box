# 核心接口契约设计

> 设计日期：2026-04-30  
> 版本：v1.0  
> 适用范围：所有插件与内核交互必须遵循本契约

---

## 设计原则

1. **接口最小化**：每个接口只做一件事，只暴露必要的方法
2. **语义清晰化**：方法名和参数不言自明，不需要额外文档
3. **依赖倒置**：插件依赖抽象接口，不依赖具体实现
4. **向后兼容**：接口变更遵循语义化版本，旧接口保留至少一个大版本

---

## 目录

1. [插件生命周期接口](#1-插件生命周期接口)
2. [内核上下文接口](#2-内核上下文接口)
3. [配置注册表接口](#3-配置注册表接口)
4. [消息总线接口](#4-消息总线接口)
5. [日志与指标接口](#5-日志与指标接口)
6. [统一错误处理规范](#6-统一错误处理规范)
7. [能力发现规范](#7-能力发现规范)
8. [WASM 沙箱 ABI](#8-wasm-沙箱-abi)
9. [标准消息主题规范](#9-标准消息主题规范)

---

## 1. 插件生命周期接口

**所有插件必须实现此接口**

```python
from abc import ABC, abstractmethod
from typing import Dict, Any, Optional

class IPlugin(ABC):
    """所有插件必须实现的基础接口"""
    
    @property
    @abstractmethod
    def plugin_id(self) -> str:
        """唯一插件ID: 反向域名风格, e.g. 'io.mqtt.v3' """
        pass
    
    @property
    @abstractmethod
    def version(self) -> str:
        """语义化版本: e.g. '1.2.3' """
        pass
    
    @property
    @abstractmethod
    def dependencies(self) -> list[str]:
        """依赖的其他插件ID列表: ['kernel.config', 'kernel.logger']"""
        pass
    
    @abstractmethod
    async def install(self, context: "IKernelContext") -> None:
        """安装阶段：注册配置、声明能力、不启动资源"""
        pass
    
    @abstractmethod
    async def start(self) -> None:
        """启动阶段：初始化连接、启动后台任务"""
        pass
    
    @abstractmethod
    async def stop(self) -> None:
        """停止阶段：优雅关闭、释放资源"""
        pass
    
    @abstractmethod
    async def uninstall(self) -> None:
        """卸载阶段：清理持久化数据"""
        pass
    
    def get_capabilities(self) -> Dict[str, Any]:
        """声明此插件提供的能力，供其他插件发现"""
        return {}
```

### 设计要点
- **两阶段启动**：`install` 和 `start` 分离，确保所有依赖就绪后才启动
- **依赖前置声明**：内核可在启动前做完整的依赖图检查
- **能力自省**：插件可动态宣告自己的能力，支持运行时发现

---

## 2. 内核上下文接口

**插件与内核交互的唯一入口**

```python
class IKernelContext(ABC):
    """插件与内核交互的唯一入口"""
    
    @property
    @abstractmethod
    def config(self) -> "IConfigRegistry":
        """配置注册表"""
        pass
    
    @property
    @abstractmethod
    def logger(self) -> "ILoggerFactory":
        """日志工厂"""
        pass
    
    @property
    @abstractmethod
    def message_bus(self) -> "IMessageBus":
        """消息总线"""
        pass
    
    @property
    @abstractmethod
    def metrics(self) -> "IMetricsRegistry":
        """度量指标"""
        pass
    
    @property
    @abstractmethod
    def scheduler(self) -> "ITaskScheduler":
        """任务调度器"""
        pass
    
    @abstractmethod
    def get_plugin(self, plugin_id: str) -> Optional[IPlugin]:
        """获取其他插件实例（用于能力调用）"""
        pass
    
    @abstractmethod
    def register_capability(self, capability: str, provider: Any) -> None:
        """注册能力，供其他插件发现和调用"""
        pass
```

### 设计要点
- **单一入口原则**：插件只知道 `IKernelContext`，不需要了解内核内部结构
- **服务定位模式**：插件按需获取服务，避免不必要的依赖注入

---

## 3. 配置注册表接口

```python
class IConfigRegistry(ABC):
    """热重载配置注册表"""
    
    @abstractmethod
    def get(self, key: str, default: Any = None) -> Any:
        """获取配置值"""
        pass
    
    @abstractmethod
    def set(self, key: str, value: Any) -> None:
        """设置配置值（触发变更通知）"""
        pass
    
    @abstractmethod
    def watch(self, key_pattern: str, callback: callable) -> None:
        """监听配置变更: key_pattern 支持通配符"""
        pass
    
    @abstractmethod
    def get_namespace(self, namespace: str) -> "IConfigRegistry":
        """获取命名空间下的子配置"""
        pass
    
    @abstractmethod
    def validate_schema(self, namespace: str, schema: Dict) -> bool:
        """验证配置是否符合 JSON Schema"""
        pass
```

### 设计要点
- **内置热重载**：插件可 `watch` 配置变更，无需重启生效
- **命名空间隔离**：每个插件在自己的命名空间下操作，避免冲突
- **Schema 校验**：配置变更时自动校验，防止错误配置导致崩溃

---

## 4. 消息总线接口

**插件间通信的唯一合法方式**

```python
class IMessageBus(ABC):
    """发布订阅消息总线"""
    
    @abstractmethod
    async def publish(self, topic: str, payload: Dict[str, Any], 
                     qos: int = 0) -> None:
        """
        发布消息
        qos: 0=最多一次, 1=至少一次, 2=恰好一次
        """
        pass
    
    @abstractmethod
    async def subscribe(self, topic_pattern: str, 
                       callback: callable[[str, Dict], None]) -> str:
        """
        订阅消息，支持通配符
        返回订阅ID，用于取消订阅
        """
        pass
    
    @abstractmethod
    async def unsubscribe(self, subscription_id: str) -> None:
        """取消订阅"""
        pass
    
    @abstractmethod
    async def request(self, service: str, payload: Dict[str, Any],
                     timeout: float = 30.0) -> Dict[str, Any]:
        """
        请求-响应模式：调用其他插件提供的服务
        同步等待响应，超时自动失败
        """
        pass
    
    @abstractmethod
    def register_service(self, service: str, handler: callable) -> None:
        """注册服务提供者，响应 request 调用"""
        pass
```

### 设计要点
- **双通信模式**：`publish/subscribe`（事件广播）+ `request/response`（服务调用）
- **MQTT 风格 QoS**：物联网场景天然适配
- **通配符订阅**：`data/sensor/#` 可匹配所有传感器数据主题

---

## 5. 日志与指标接口

```python
class ILoggerFactory(ABC):
    """统一日志工厂"""
    
    @abstractmethod
    def for_plugin(self, plugin_id: str) -> "IPluginLogger":
        """为指定插件创建日志实例"""
        pass

class IPluginLogger(ABC):
    @abstractmethod
    def debug(self, message: str, **kwargs): pass
    
    @abstractmethod
    def info(self, message: str, **kwargs): pass
    
    @abstractmethod
    def warn(self, message: str, **kwargs): pass
    
    @abstractmethod
    def error(self, message: str, exception: Exception = None, **kwargs): pass
    
    @abstractmethod
    def with_fields(self, **fields) -> "IPluginLogger":
        """创建带上下文字段的子日志实例"""
        pass


class IMetricsRegistry(ABC):
    """统一度量指标接口（Prometheus 兼容）"""
    
    @abstractmethod
    def counter(self, name: str, labels: Dict[str, str] = None) -> "ICounter":
        """计数器：只增不减"""
        pass
    
    @abstractmethod
    def gauge(self, name: str, labels: Dict[str, str] = None) -> "IGauge":
        """仪表盘：可增可减"""
        pass
    
    @abstractmethod
    def histogram(self, name: str, labels: Dict[str, str] = None) -> "IHistogram":
        """直方图：统计分布（延迟、大小等）"""
        pass
```

### 设计要点
- **结构化日志**：`with_fields` 支持带上下文的日志
- **标准化指标**：三种基础指标类型完全兼容 Prometheus 生态
- **无依赖原则**：内核不绑定具体日志/指标实现，由插件提供后端

---

## 6. 统一错误处理规范

> 本节定义全系统的错误处理标准，所有插件必须遵循

### 9.1 错误分类与错误码规范

错误码采用 **层级结构**：`{类别}-{模块}-{具体错误}`

| 一级类别 | 说明 | 示例 |
|---------|------|------|
| `E-SYS` | 系统级错误（内核、资源耗尽） | `E-SYS-MEM` 内存不足, `E-SYS-DISK` 磁盘满 |
| `E-CFG` | 配置错误 | `E-CFG-INVALID` 配置格式错误, `E-CFG-MISSING` 配置缺失 |
| `E-IO` | 数据IO错误 | `E-IO-CONN` 连接失败, `E-IO-TIMEOUT` 超时, `E-IO-AUTH` 认证失败 |
| `E-LLM` | LLM推理错误 | `E-LLM-RATELIMIT` 限流, `E-LLM-MODEL` 模型不可用, `E-LLM-TOKEN` 超限 |
| `E-FENCE` | 逻辑围栏错误 | `E-FENCE-DENY` 规则拦截, `E-FENCE-SANDBOX` 沙箱异常 |
| `E-PLUGIN` | 插件错误 | `E-PLUGIN-LOAD` 加载失败, `E-PLUGIN-DEP` 依赖缺失 |

### 9.2 标准错误对象

```python
from dataclasses import dataclass
from enum import IntEnum
from typing import Optional, Dict

class ErrorLevel(IntEnum):
    """错误严重级别"""
    DEBUG = 0       # 调试信息，不影响运行
    WARNING = 1     # 警告，可自动恢复
    ERROR = 2       # 错误，需人工介入但系统仍可运行
    CRITICAL = 3    # 致命，系统必须停止或重启

@dataclass
class PluginError:
    """标准错误对象，所有插件必须返回此类型"""
    code: str                      # 错误码，见上表
    message: str                   # 人类可读描述
    level: ErrorLevel              # 严重级别
    plugin_id: str                 # 产生错误的插件ID
    timestamp: int                 # 发生时间戳 ms
    retryable: bool = False        # 是否可重试
    cause: Optional[str] = None    # 根因错误（异常堆栈字符串）
    context: Dict[str, str] = None # 上下文信息（设备ID、请求ID等）
```

### 9.3 错误传播机制

#### 方式一：同步返回（推荐）
```python
# 可能出错的方法返回 Result 类型
from typing import Generic, TypeVar, Union

T = TypeVar('T')
Result[T] = Union[T, PluginError]

class IDataStore:
    async def get(self, key: str) -> Result[Optional[bytes]]:
        """获取数据，成功返回 bytes，失败返回 PluginError"""
        pass
```

#### 方式二：错误事件广播（异步错误）
通过消息总线发布到标准错误主题：

| 主题 | 用途 |
|------|------|
| `kernel/error/{plugin_id}` | 插件发布自身错误 |
| `kernel/error/all` | 内核转发所有错误（用于监控） |

```python
# 插件发布错误示例
async def on_some_error(self, error: PluginError):
    await self.context.message_bus.publish(
        topic=f"kernel/error/{self.plugin_id}",
        payload={
            "code": error.code,
            "message": error.message,
            "level": error.level.value,
            "retryable": error.retryable,
            "context": error.context or {},
        },
        qos=1  # 错误事件至少送达一次
    )
```

### 9.4 熔断与降级策略

#### 熔断器接口
```python
class ICircuitBreaker(ABC):
    """熔断器：连续失败时快速失败，保护下游"""
    
    @abstractmethod
    def can_execute(self, operation: str) -> bool:
        """是否允许执行此操作（熔断器关闭返回True，打开返回False）"""
        pass
    
    @abstractmethod
    def record_success(self, operation: str) -> None:
        """记录成功，连续成功时自动恢复"""
        pass
    
    @abstractmethod
    def record_failure(self, operation: str) -> None:
        """记录失败，连续失败时打开熔断器"""
        pass
    
    @abstractmethod
    def on_half_open(self, operation: str, callback: callable) -> None:
        """半开状态探测回调"""
        pass
```

#### 默认熔断配置
| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 失败阈值 | 5次 | 连续失败5次后打开熔断器 |
| 冷却时间 | 30秒 | 打开30秒后进入半开状态探测 |
| 半开探测次数 | 3次 | 半开时允许3次探测请求 |

#### 降级机制规范
当熔断器打开时，插件必须提供降级逻辑：
```python
# LLM 插件降级示例
async def chat_completion(self, prompt):
    if not self.circuit_breaker.can_execute("openai_api"):
        # 降级：使用本地小模型，或返回缓存结果
        return await self.local_llm.chat(prompt)
    
    try:
        result = await self.openai_api.call(prompt)
        self.circuit_breaker.record_success("openai_api")
        return result
    except Exception as e:
        self.circuit_breaker.record_failure("openai_api")
        raise
```

### 9.5 错误处理最佳实践

1. **失败有界**：任何失败不能导致整个系统崩溃，影响范围必须隔离在插件内
2. **快速失败**：检测到不可恢复状态时立即返回错误，不要排队堆积
3. **失败可观测**：所有错误必须结构化记录，包含足够排查问题的上下文
4. **自动恢复**：只要可能，提供重试或降级逻辑，不需要人工介入
5. **不吞错误**：catch 住的异常必须至少记录一条 error 日志，不能静默忽略

---

## 7. 能力发现规范

### ❌ 反模式（紧耦合，禁止使用）
```python
# 不要直接导入和依赖具体实现
from llm.openai import OpenAIPlugin

class MyPlugin:
    def __init__(self):
        self.llm = OpenAIPlugin()  # ❌ 紧耦合
```

### ✅ 正确方式（松耦合，能力发现）
```python
class MyPlugin(IPlugin):
    async def install(self, context: IKernelContext):
        # 通过能力接口发现 LLM 服务
        self.llm_provider = context.get_capability("llm.completion")
        # llm_provider 是一个接口，具体实现是谁完全不重要
    
    async def do_something(self):
        # 调用标准接口，不关心是谁实现的
        result = await self.llm_provider.complete(prompt="...")
```

### 标准能力命名空间
| 能力名称 | 接口契约 | 用途 |
|---------|---------|------|
| `llm.completion` | `ILLMCompletion` | 文本补全 |
| `llm.chat` | `ILLMChat` | 对话补全 |
| `llm.embedding` | `ILLMEmbedding` | 向量嵌入 |
| `data.store` | `IDataStore` | 数据持久化 |
| `data.query` | `IDataQuery` | 数据查询 |

---

## 8. WASM 沙箱 ABI

Logic Fence 插件专用：用户自定义代码运行在 WASM 沙箱中，通过固定 ABI 与宿主交互。

### 宿主导出函数（WASM 可调用）
```c
// 数据读写
int32_t read_data(const char* key, char* buf, int32_t buf_len);
int32_t write_data(const char* key, const char* value, int32_t value_len);

// 事件发布
int32_t publish_event(const char* topic, const char* payload, int32_t payload_len);

// 日志输出
void log_debug(const char* message);
void log_info(const char* message);
void log_error(const char* message);

// 时间获取
int64_t get_timestamp_ms();
```

### WASM 必须导出函数
```c
// 初始化
int32_t fence_init();

// 处理单个事件，返回是否允许通过
bool fence_process_event(const char* event_type, const char* payload);

// 清理
void fence_cleanup();
```

### 设计要点
- **极简 ABI**：只有 8 个函数，任何可编译到 WASM 的语言都能使用
- **能力受限**：沙箱内代码不能直接访问网络、文件系统
- **全量审计**：所有交互都通过宿主代理，内核可审计沙箱的每一个操作

---

## 9. 标准消息主题规范

| 主题模式 | 用途 | 载荷格式 |
|---------|------|---------|
| `data/input/{protocol}/{device_id}` | 设备数据输入 | 标准化数据帧 |
| `data/output/{protocol}/{device_id}` | 设备控制输出 | 控制指令 |
| `llm/request/{model_family}` | LLM 推理请求 | 标准推理格式 |
| `llm/response/{request_id}` | LLM 推理响应 | 推理结果 |
| `fence/alert/{rule_id}` | 规则引擎告警 | 告警详情 |
| `plugin/{plugin_id}/event/{event_type}` | 插件内部事件 | 自定义 |
| `kernel/system/{event_type}` | 内核系统事件 | 启动/停止/错误等 |

---

## 接口契约总览

```
┌─────────────────────────────────────────────────────────┐
│                   插件代码（业务逻辑）                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ MQTT     │  │ 规则引擎  │  │ LLM      │              │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘              │
└───────┼──────────────┼──────────────┼────────────────────┘
        │              │              │
        └──────────────┼──────────────┘
                       ▼
        ┌───────────────────────────────────┐
        │      标准接口契约层                │
        │  IPlugin / IKernelContext         │
        │  IMessageBus / IConfigRegistry    │
        │  ILogger / IMetrics               │
        └───────────────┬───────────────────┘
                        ▼
        ┌───────────────────────────────────┐
        │         微内核实现                  │
        │  (只有这一层需要维护核心代码)        │
        └───────────────────────────────────┘
```

---

*本文件为架构核心文档，变更需谨慎*
