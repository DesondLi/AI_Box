# ============================================================================
# 微内核项目 - 开发环境依赖
# ============================================================================

# ----- 测试框架 -----
pytest>=7.0                    # 测试运行器
pytest-asyncio>=0.21          # asyncio 测试支持
pytest-cov>=4.0               # 代码覆盖率统计

# ----- 代码质量 -----
ruff>=0.2                     # 超快速 lint + format（替代 flake8 + isort + black）
mypy>=1.8                     # 类型检查（渐进式类型化）

# ----- 工具链 -----
ipython>=8.0                  # 增强型 Python Shell（调试用）
pre-commit>=3.0               # Git 提交钩子（可选，代码门禁）

# ----- 文档 -----
# mkdocs-material              # 如需文档站点，取消注释

