# AI盒子 - 通用微内核架构项目记忆

> **创建日期**：2026-04-30  
> **最后更新**：2026-05-06  
> **当前状态**：核心实现阶段 - 微内核原型已可运行，插件生命周期完整实现  
> **项目目标**：设计一套面向物联网全场景的通用软件架构，支持可配置逻辑围栏、数据IO抽象、LLM加速与热拔插

---

## 📋 项目背景

从 `dual-track-memory-demo`（双轨记忆系统）项目实践中总结经验，抽象出一套通用架构框架。

---

## 🎯 关键决策记录

### 决策 1：部署场景范围（2026-04-30）
**问题**：架构需要支持哪些部署场景？
- **选择**：D - 全场景覆盖（从超轻量嵌入式设备到云端集群）
- **理由**：物联网场景差异巨大，单一架构必须支持弹性伸缩和按需裁剪
- **影响**：微内核必须极致精简（< 500KB），所有功能通过插件方式加载

### 决策 2：逻辑围栏实现方式（2026-04-30）
**问题**：如何实现安全/合规边界控制？
- **选择**：C - 声明式规则 + 沙箱插件 双轨结合
- **理由**：简单规则用 YAML 配置快速落地，复杂逻辑用 WASM 沙箱隔离执行
- **影响**：需要同时实现规则引擎和 WASM 运行时

### 决策 3：LLM 核心优先级（2026-04-30）
**问题**：LLM 层的设计优先级？
- **选择**：D - 三者并重（性能 + 灵活 + 成本）
- **理由**：性能保证实时响应，灵活保证模型切换，成本保证商业可行性
- **影响**：需要实现推理加速引擎、多模型适配器、智能路由与缓存三层结构

### 决策 4：数据 IO 抽象特性（2026-04-30）
**问题**：数据层需要支持哪些特性？
- **选择**：D - 协议抽象 + 本地优先 + 数据分级 + 插件扩展
- **理由**：物联网协议碎片化严重，网络不可靠是常态，数据价值差异大
- **影响**：需要设计统一数据接口，内置断点续传和分级队列机制

### 决策 5：架构范式选择（2026-04-30）
**问题**：采用哪种架构范式？
- **选择**：方案A - 微内核架构（推荐）
- **备选**：六边形架构、数据流架构
- **理由**：
  1. 完美匹配全场景覆盖需求
  2. LLM热拔插、协议扩展自然适配
  3. 与 dual-track-memory-demo 的"协调器 + 多模块"思想一脉相承
- **影响**：核心工作集中在接口契约设计和插件运行时实现

### 决策 6：时序数据语义桥接层（2026-05-06）
**问题**：物联网海量时序数据如何高效喂给 LLM？
- **选择**：RAG/Memory 语义桥接层 - 时序数据 → 自然语言摘要
- **实现策略**：Level 0 规则模板（无依赖）→ Level 1 本地小模型 → Level 2 云端大模型
- **理由**：
  1. 数据量爆炸：1000 传感器 × 1 分钟采样 = 144 万条/天，直接送 LLM 不可能
  2. Token 成本：300 浮点数 ≈ 600 token → 语义摘要 ≈ 30 token，压缩比 20:1
  3. 渐进式增强：从无依赖规则模板开始，按需升级模型复杂度
- **影响**：LLM 获得高质量低 token 消耗的状态上下文，Agent 决策效率大幅提升

### 决策 6：Agent 编排集成（2026-04-30）
**问题**：LangGraph 和 Agent-Skill 如何集成？
- **选择**：作为第6层 Agent 应用层的核心组件
- **定位**：
  - LangGraph：工作流内核插件，负责状态图编排
  - Skill 系统："插件的插件"，每个 Skill 是 LangGraph 节点的具体实现
- **理由**：分层清晰，不需要 Agent 能力的场景可以完全不加载这一层

---

## 🏗️ 架构设计进展

### ✅ 已完成：整体架构分层（6层）

| 层级 | 名称 | 核心组件 | 大小估算 |
|------|------|---------|---------|
| 第1层 | 微内核 Core | 配置注册表、标准接口契约、基础工具库、统一错误、日志抽象、度量接口、内存管理、时间调度 | < 500KB |
| 第2层 | 核心功能插件 | Data IO 插件组、Logic Fence 插件组、LLM Engine 插件组 | 每个插件 1-10MB |
| 第3层 | 插件运行时 | 生命周期管理器、依赖注入容器、消息路由、安全沙箱 | ~5MB |
| 第5层 | 服务网关层 | 统一API网关、事件总线、服务发现、可观测性 | ~20MB |
| 第6层 | Agent 应用层 | LangGraph引擎、Skill注册表、工作流编排器、行业应用包 | ~50MB |

> **注意**：第4层预留，可用于分布式协调/共识层

### ✅ 已完成：Mermaid 架构图
- 保存位置：`01-架构设计-微内核架构图.md`
- 渲染支持：GitHub / GitLab / Obsidian / Notion / FigJam

### ✅ 已完成：核心接口契约设计（2026-04-30）
- 保存位置：`02-核心接口契约设计.md`
- 完成内容：
  1. **IPlugin** - 插件生命周期接口（install/start/stop/uninstall）
  2. **IKernelContext** - 内核上下文（插件与内核交互的唯一入口）
  3. **IConfigRegistry** - 热重载配置注册表
  4. **IMessageBus** - 消息总线（publish/subscribe + request/response）
  5. **ILoggerFactory / IMetricsRegistry** - 日志与度量接口
  6. **统一错误处理规范** - 错误码分类、标准错误对象、传播机制、熔断降级策略
  7. **能力发现规范** - 插件间能力调用的松耦合机制
  8. **WASM 沙箱 ABI** - Logic Fence 自定义代码的宿主接口
  9. **标准消息主题规范** - MQTT 风格主题命名空间
- 设计原则：接口最小化、语义清晰、依赖倒置、向后兼容

### 📝 设计自评审结论（2026-04-30）
- ✅ 架构分层清晰，依赖方向正确
- ✅ 接口职责单一，没有过度设计
- ✅ 错误处理规范已补充：统一错误码 + Result 返回类型 + 事件广播 + 熔断器接口
- ⚠️ 流控与背压、持久化抽象、版本兼容性等留到实现阶段逐步细化
- 🎯 当前设计已足够支撑内核原型开发，可以进入编码阶段

---

## 💻 核心实现进展（2026-05-06）

### ✅ 已完成：微内核核心实现

**目录结构**：
```
kernel/
├── interfaces/              # 抽象接口层（插件仅依赖这层）
│   ├── plugin.py            # IPlugin 插件生命周期接口
│   ├── kernel_context.py    # IKernelContext 内核上下文
│   ├── config.py            # IConfigRegistry 配置注册表
│   ├── message_bus.py       # IMessageBus 消息总线
│   ├── logger.py            # ILoggerFactory 日志工厂
│   ├── errors.py            # PluginError 统一错误处理
│   └── __init__.py
├── core/                    # 内核核心实现
│   ├── kernel.py            # MicroKernel 主入口
│   ├── plugin_registry.py   # 插件注册表 + 依赖拓扑排序
│   ├── memory_message_bus.py# 内存消息总线（pub/sub + req/resp）
│   ├── memory_config.py     # 内存配置注册表
│   ├── console_logger.py    # 控制台日志实现
│   └── __init__.py
├── plugins/                 # 参考插件
│   ├── base_plugin.py       # 插件基类
│   ├── mqtt_plugin.py       # MQTT 输入插件（模拟数据）
│   └── __init__.py
├── demo.py                  # 简单演示
└── __init__.py
```

### ✅ 已实现核心功能

| 组件 | 状态 | 测试覆盖率 | 说明 |
|------|------|-----------|------|
| **RAG/Memory 语义桥接器** | ✅ Level 0 完成 | 98% | 时序数据 → 自然语言摘要，9:1 Token 压缩比 |
| 插件生命周期 | ✅ 完整 | 98% | install/start/stop/uninstall 四阶段 |
| 依赖解析 | ✅ 完整 | 98% | 拓扑排序，支持循环依赖检测 |
| 消息总线 | ✅ 完整 | 98% | MQTT 3.1.1 规范：`+` 单层 / `#` 多层通配符 |
| 内核核心 | ✅ 可用 | 95% | 插件上下文、启动/停止流程 |
| 日志抽象 | ✅ 完整 | 91% | 控制台实现，支持插件分级日志 |
| 配置注册表 | ⚠️ 可用 | 38% | 内存实现，缺少热重载测试 |
| 能力发现 | ✅ 可用 | - | register_capability / get_capability |
| 统一错误 | ✅ 完整 | 82% | PluginError + ErrorLevel + Result 类型 |

### ✅ 已完成参考插件：MQTT Input Plugin
- 模拟设备数据发布（sensor-001, sensor-002, gateway-001）
- 注册 echo 服务演示 request/response 模式
- 演示标准插件开发规范

### 🧪 运行验证
```bash
python run_demo.py  # 完整演示脚本
# 输出：插件注册 → 依赖解析 → 安装 → 启动 → 服务调用 → 数据流动 → 停止
```

### ⚠️ 已知问题
- Windows GBK 编码：已通过 UTF-8 重配置修复
- 消息总线通配符：✅ 已完成完整 MQTT 3.1.1 规范支持
- 配置：缺少热重载和持久化功能

---

## 🎨 设计原则

### 依赖倒置原则
- 箭头方向 = 依赖方向 = 调用方向
- 上层依赖下层，下层不依赖上层
- 所有业务插件只依赖内核抽象
- 内核完全不感知上层业务

### 一切皆插件
- 从通信协议到 AI 模型，所有功能都是可插拔的
- 运行时中立：内核不绑定任何特定执行环境
- 热拔插原生支持：安装/启动/停止/升级 不影响运行

### 场景化按需加载
- **超轻量嵌入式**：仅内核 + MQTT + 规则引擎 (~2MB)
- **边缘网关**：全量 IO + 本地 LLM + Logic Fence (~50MB)
- **云端集群**：全量功能 + LangGraph + Skill 生态 (~200MB)

---

## 📝 下一步计划

### 当前迭代：测试覆盖与质量加固（高优先级）
- [ ] **单元测试脚手架**：为核心组件编写 pytest 测试用例
  - MicroKernel 生命周期测试
  - PluginRegistry 依赖解析测试
  - MemoryMessageBus 消息模式测试
  - 错误处理与边界条件测试

### 下阶段：核心功能增强
- **消息总线增强**：完整 MQTT 风格通配符支持（`+` / `#`）
- **配置热重载**：配置变更事件 + 回调机制
- **WASM 沙箱运行时**：集成 wasmtime 作为 Logic Fence 插件
- **持久化抽象**：配置与状态的磁盘持久化接口

### 待启动：第二批参考插件
- **简单规则引擎**（Logic Fence 组参考实现）
  - YAML 声明式规则配置
  - 条件匹配 → 动作执行 流水线
- **OpenAI 适配器**（LLM Engine 组参考实现）
  - 统一 LLM 调用接口
  - 响应缓存与成本优化

### 中远期：
- 插件包格式与分发机制（.aipkg）
- 行业应用包开发指南
- 性能基准测试套件
- 安全审计规范（WASM 沙箱权限模型）

---

## 📁 项目文件索引

```
D:/AI盒子/
├── memory.md                            ✅ 本文件（项目记忆）
├── 01-架构设计-微内核架构图.md          ✅ 架构图
├── 02-核心接口契约设计.md              ✅ 接口规范
├── run_demo.py                          ▶️ 完整演示脚本
├── kernel/                              📦 微内核核心代码
│   ├── interfaces/                      📄 抽象接口定义
│   ├── core/                            📄 内核核心实现
│   └── plugins/                         📄 参考插件实现
├── AI盒子选型.md                        📄 硬件选型参考
├── 算力盒子.xlsx
├── md_to_word.py                        🛠️ 工具脚本
└── update_doc.py                        🛠️ 工具脚本
```

---

*本文件随项目进展自动更新*
