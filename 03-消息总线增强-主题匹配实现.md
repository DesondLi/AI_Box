# 消息总线增强：MQTT 主题匹配实现

## 概述

实现符合 MQTT 3.1.1 规范的主题匹配算法，替代原有基于 `fnmatch` 的简单替换实现。

---

## 问题背景

原有实现的问题：
1. `+` 通配符被简单替换为 `*`，导致跨层级匹配（如 `sensor/+` 错误地匹配 `sensor/temp/value`）
2. `#` 通配符匹配逻辑不严谨，不支持零层级匹配（如 `sensor/#` 不匹配 `sensor`）
3. 缺少严格的层级数量检查

---

## MQTT 主题匹配规范

### 核心规则

| 通配符 | 语义 | 约束 |
|--------|------|------|
| `+` | 单层通配符 | 精确匹配**一个**层级，不能多也不能少 |
| `#` | 多层通配符 | 匹配**零个或多个**层级，**必须在主题末尾** |
| `/` | 层级分隔符 | 前导或末尾的 `/` 会创建空层级 |

### 示例

```
模式: sensor/+
  ✓ sensor/temp
  ✓ sensor/humidity
  ✗ sensor           (层级不够)
  ✗ sensor/temp/val  (层级过多)

模式: sensor/#
  ✓ sensor
  ✓ sensor/temp
  ✓ sensor/temp/room1
  ✓ sensor/temp/room1/value

模式: +/#
  ✓ sensor/temp
  ✓ sensor/temp/room1/value

模式: building/+/floor/+/sensor/#
  ✓ building/a/floor/3/sensor/temp/value
```

---

## 算法实现

### 核心逻辑

```python
def _match_topic(pattern: str, topic: str) -> bool:
    # 1. 快速路径：精确匹配
    if pattern == topic:
        return True

    # 2. 空主题特殊处理：只有 # 能匹配空
    if topic == "":
        return pattern == "#"

    # 3. 按层级分割
    pattern_parts = pattern.split('/')
    topic_parts = topic.split('/')

    # 4. 处理 # 通配符
    has_hash = False
    if pattern_parts and pattern_parts[-1] == '#':
        has_hash = True
        pattern_parts = pattern_parts[:-1]

    # 5. 层级数量检查
    if not has_hash and len(pattern_parts) != len(topic_parts):
        return False
    if has_hash and len(topic_parts) < len(pattern_parts):
        return False

    # 6. 逐层级匹配
    for p_part, t_part in zip(pattern_parts, topic_parts):
        if p_part == '+':
            continue  # + 匹配任意单个层级
        if p_part != t_part:
            return False

    return True
```

---

## 测试覆盖

新增 `tests/test_topic_matching.py`，共 15 个测试用例：

| 测试分类 | 用例数 | 覆盖场景 |
|---------|--------|---------|
| 精确匹配 | 1 | 无通配符的精确匹配 |
| # 通配符 | 3 | 结尾匹配、零层级匹配、多场景 |
| + 通配符 | 3 | 单层匹配、多层匹配、精确层级约束 |
| 组合使用 | 1 | `+` 与 `#` 组合 |
| 边界情况 | 5 | 前导/末尾 `/`、大小写、空主题、$ 系统主题 |
| 真实场景 | 2 | IoT 设备遥测模式、楼层传感器模式 |

---

## 验证结果

```
============================= 48 passed in 0.35s ==============================
```

- 原有 33 个测试全部通过（无回归）
- 新增 15 个主题匹配测试全部通过

---

## 文件变更

| 文件 | 变更内容 |
|------|---------|
| `kernel/core/memory_message_bus.py` | 重写 `_match_topic` 方法 |
| `tests/test_topic_matching.py` | 新增 15 个测试用例 |
| `memory.md` | 更新已知问题状态 |
